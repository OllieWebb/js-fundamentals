1. First-class functions have the functionality to be passed as an argument into another function, returned by another fucntion or can be assigned as a value to a variable. In Javascript in particular fucntions are treated as first-class citizens, which means that functions are treated as a value and have the same functionaloity as other objects. When declaring a function, the functionality of that function is defined but it is only executed when the function is called in Main().